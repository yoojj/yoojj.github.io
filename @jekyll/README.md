# Jekyll
: 텍스트 변환 엔진  
: 마크업 언어나 마크다운으로 작성한 문서를 설정한 규칙에 맞춰 정적 사이트를 생성   


```bash
$ yum install ruby
$ gem install jekyll bundler


# gemfile 생성
$ bundle init

# jekyll 추가
$ bundle add jekyll

# 정적 사이트 생성
$ jekyll build

# 변화를 감지해 재생성
$ jekyll build --watch

# 실행
$ jekyll serve
```



[top](#)
